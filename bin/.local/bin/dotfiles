#!/bin/bash

set -e


pushd "$HOME/dotfiles"

for i in $(ls -d */ | grep -vE "^_"); do
    echo "$i"
    stow -t ~ -D "$i";
    stow -t ~ -S "$i";
done

ln -sf "Syncthing/conf/.histfile" "$HOME/.histfile"
ln -sfn "Syncthing/bin" "$HOME/bin"

popd
